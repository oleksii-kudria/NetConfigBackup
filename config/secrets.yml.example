# Example secrets structure without real credentials
# Copy to config/secrets.yml and fill with real passwords.
# Secrets are referenced from devices.yml via auth.secret_ref and can
# be overridden with environment variables
# NETCONFIGBACKUP_SECRET_<SECRET_REF>.

secrets:
  hq-core-sw1:
    password: "CHANGE_ME"
  travel-mikrotik:
    password: "CHANGE_ME"
