# Example secrets structure without real credentials
# Copy to config/secrets.yml and provide usernames/passwords.

devices:
  core-switch:
    username: cisco-admin
    password: CHANGEME
  edge-router:
    username: tik-admin
    password: CHANGEME
